import { DndUploaderDirective } from "./dnd-uploader.directive";

describe("DndUploaderDirective", () => {
  it("should create an instance", () => {
    const directive = new DndUploaderDirective();
    expect(directive).toBeTruthy();
  });
});
